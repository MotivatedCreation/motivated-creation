/* global document */

$(document).ready(function()
{
    var alert = $("<%= escape_javascript(render(:partial => './layouts/alert', :object => flash[:error])) %>").appendTo('.container');
    openAlert();
});