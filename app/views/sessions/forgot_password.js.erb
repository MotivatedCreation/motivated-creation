/* global document */

$(document).ready(function()
{
    var overlay = $('.overlay');
    overlay.show();
    
    var forgot_password_modal = $("<%= escape_javascript(render(:partial => './layouts/forgot-password')) %>").appendTo('.container');
});