/* global document */

$(document).ready(function()
{
    var overlay = $('.overlay');
    overlay.show();
    
    var signup_modal = $("<%= escape_javascript(render(:partial => './layouts/signup')) %>").appendTo('.container');
});